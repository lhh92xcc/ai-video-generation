# TTS 发音词典。这里的替换只作用于发送给 TTS Provider 的文本，
# 字幕和剧本仍保留原字形。
# 多条规则会按 source 长度从长到短执行；请优先填写完整人名或词组，
# 避免短规则抢先替换多音字词组。变更后重新创建旁白任务才会生效。
#
# 常见用途：人名、多音字、英文品牌名、缩写。请用“能让当前音色读对”的
# 中文替代表达，不要填写拼音标注。例如：
# "重明" = "崇明"
# "长安" = "常安"

[replacements]
